import {EventTypes} from "@/shared/types";

export const EVENTS_NAV_ORDER: EventTypes[] = [
  EventTypes.Sentry,
  EventTypes.Profiler,
  EventTypes.Smtp,
  EventTypes.Sms,
  EventTypes.HttpDump,
  EventTypes.Inspector,
  EventTypes.VarDump,
  EventTypes.Monolog,
  EventTypes.RayDump,
]
